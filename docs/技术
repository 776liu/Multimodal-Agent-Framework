已知局限:

1. 多会话隔离问题
    - 现象：在 Streamlit 前端中切换多个逻辑会话时，任务结果可能串到错误的会话窗   口。

    - 根因：Streamlit 的脚本重执行模型 + 单一 st.session_state 设计，无法在一个浏览器 Tab 内实现真正的多会话状态隔离。当用户切换会话时，前端需要在同一个  st.session_state 中保存和恢复不同会话的数据，时序错乱会导致渲染残留。

    - 当前方案：降级为单会话模式。Streamlit 只维护一个活跃会话，用户通过“新建会话”或“删除历史”来切换上下文。

    - 后续升级：若需多会话能力，需将前端迁移至 React 或 Vue。后端 API 层已完全支持多会话（session_id 参数在所有端点中可用），无需修改。

2. 异步任务依赖问题
    - 现象：用户连续提交多条依赖前序结果的指令（如“生成小猫图片” → “描述这张图片”），后续任务在 PLANNING 阶段直接失败（call_chain 为空）。

    - 根因：异步架构将每条指令视为独立任务并立即入队。后续任务被 Worker 消费时，前序任务可能尚未完成，导致依赖数据（如图片 URL）不可用。TaskRouter 无法生成有 效执行计划，Agent 直接返回 FAILED。

    - 当前方案：前端交互层限制——提交任务后禁用“开始执行”按钮，直至当前任务完成。保证用户不会在前序任务完成前提交依赖任务。

    - 后续升级：见“架构演进”章节。

3. PLANNING 阶段失败的日志盲区
    - 现象：当任务在 PLANNING 阶段失败时，log_record 中 call_chain 和 error_summary 均为空，无法从日志中直接判断失败原因。

    - 根因：call_chain 只在调用模型后才追加记录，PLANNING 阶段的失败没有对应的日志字段。
  
    - 当前方案：新增 _log_planning_failure 方法，在 PLANNING 失败时向 error_summary 写入 {"step": 0, "type": "PLANNING_FAILED", "reason": "..."}，覆盖日志盲区。

========================================================================
架构演进:

方案一：Worker 层任务依赖声明
    - 目标：支持任务间的显式依赖关系，Worker 在消费任务时自动等待前序任务完成。

    - 设计要点：

        TaskRequest 新增 depends_on: Optional[str] 字段，值为依赖的 task_id
    
        Worker 在 dequeue 拿到任务后，若存在 depends_on 字段，先轮询依赖任务的状态（GET /api/task/{depends_on}），直至终态后才开始执行当前任务
    
        若依赖任务最终状态为 FAILED，当前任务直接标记为 FAILED，附带错误信息 {"error": "Dependency task failed"}
    
        若依赖任务超时（超过预设等待时间），当前任务标记为 FAILED，附带错误信息 {"error": "Dependency task timeout"}

    - 优点：依赖关系显式化，可实现任务 DAG 编排，扩展性强。

    - 缺点：Worker 逻辑复杂度增加，需要处理循环依赖检测、超时、依赖任务不存在等边界情况。

方案二：Agent 层上下文记忆增强
    - 目标：Agent 在执行新任务时，自动从 Storage 中获取前序成功任务的产出（图片 URL、文本结果），注入当前任务的上下文。

    - 设计要点：

        Agent 在 PLANNING 阶段，不仅调用 Memory.build_context() 获取对话历史，还调用 Storage.get_last_task_input(session_id) 获取最近一次成功任务的产出

        将前序产出（图片 URL、文本内容）作为上下文，注入当前任务的第一个子任务的 prompt中

        子任务间图片传递逻辑由 Agent 在遍历执行计划时自动完成（已部分实现：agent.py 中已有图片 URL传递逻辑）

    - 优点：前端和 Worker 完全无感知，是多轮对话最自然的体验。用户不需要关心任务依赖，Agent 自己“记住”上下文。

    - 缺点：Agent 核心逻辑改动较大，需要重新定义“上下文”的边界（哪些历史数据应该被注入？注入格式是什么？）。若前序任务失败，后续任务需要独立的降级策略。




