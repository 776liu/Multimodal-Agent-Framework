完整架构：
    [前端请求]
        |
        | HTTP (POST /api/task/submit, GET /api/task/{task_id})
        V 
    [FastAPI]—— LPUSH ——> [Redis List "agent: queue"]
                                    | BLPOP 
                                    V 
                                [worker]
                                    | 调用 agent.run(user_input, session_id)
                                    V
                            +--核心引擎模块--+
                            |    TaskRouter   
                            |    Router
                            |    LLMClient
                            |    Builder    ————>[SQLite]
                            |    Memory     ————>[SQLite]
                            |    Agent
                            +---------------+
                                    |
                                    |
                                    V 
                            [Redis Hash "agent:task{task_id}"]
                            (更新 status="success/failsed", result=,,)
                                    ^
                                    | HGET
                                [FastAPI]
                                    |
                                    |   HHTTP轮询
                                    V 
                                [前端]

在核心引擎外面加了一层异步任务调度。Worker 是独立进程，通过 Redis 队列解耦前后端。
异步任务调度与核心引擎模块是同步执行，只是调用方从前端请求变成了 Worker。  
--------------------------------------------------------------
核心模块：
    [用户请求] --> Agent
                    |
                    v
                TaskRouter (拆解任务)
                    |
                    v (子任务列表)
                Agent (遍历执行计划)
                    |
                    v (子任务 + 需求标签)
                Router (匹配模型)
                    |
                    v (可用的模型地址)
                llmClient (发起HTTP调用)
                    |
                    v (原始结果/错误码)
                Agent (收集结果, 遇错则调Router再要地址)
                    |
                    v (所有原始结果)
                Builder (拼装JSON) -> Storage（存入SQLite持久化）
                    |
                    V
                 Memory（存入会话记忆）
                    |
                    v (统一响应)
    [前端] <--  Agent

-----------------------------------------------------------------
异步请求链： 
    [前端请求] --> FastAPI
                    |
                    v
                RedisList(任务队列, agent:queue)
                |
                V(Redis LPUDH/BLPOP)
                RedisHash(任务状态存储, agent:task: {task_id})
                |
                V (队列任务)
                Worker
                |
                V 
                Agent(执行) —> SQLite(写入日志)
                |
                V 
                Redis(更新状态)


================================================
================================================

模型确认：
    注册名          标签                   百炼模型                用途
    qwen-plus      text-generation      qwen3.7-plus            多模态输入生成文字（9.1过期）
    qwen-max       text-generation      qwen3.7-max-2026-06-08  文字输入生成文字（9.8过期）
    qwen-image     image-generation     qwen-image-2.0-pro      文字图片生成图片（7.23过期）
    wan2.7         image-generation     wan2.7-image-pro        文字图片生成图片（7.1过期）
    qwen3.5-plus   text-generation      qwen3.5-plus-2026-04-20 文字图片生成文字（7.23）

==================================================
==================================================
模块名：Router
职责：模型注册、能力匹配、备选切换

输入：
    - capability_required: string（如 "text-generation","image-generation"）
    - failed_models: list[string]（可选，已失败的模型名列表）

输出：
    - 成功：{
        "model_name": "model-x", 
        "endpoint": "http://...", 
        "api_key": "..."
        }
    - 失败：{
        "error": "NO_AVAILABLE_MODEL"
        }

核心逻辑：
    1. 从注册表中查找所有具备capability_required能力的模型
    2. 过滤掉failed_models中已失败的模型
    3. 在剩余模型中按优先级排序（先注册的优先）
    4. 返回第一个可用模型的连接信息
    5. 如果无可用模型，返回NO_AVAILABLE_MODEL错误


-------------------------------------------------------------
模块名：Agent
职责：总控，面向用户，负责驱动状态机

输入：
    - user_input: string (用户输入的文本)

输出：
    - 成功：
   {
        "task_id": "uuid-xxxx",
        "task_status": "SUCCESS",
        "system_status": "READY",
        "data": { ... }
    }
    - 失败：
      {
        "task_id":"uuid-xxxx",
        "task_status":"FAILED",
        "system_status": "READY", 
        "data": {
            "type":"text",
            "message": "服务暂时不可用，请稍后重试"
            }
    }

核心逻辑：
    核心逻辑（按状态机流转描述）：

[状态: READY]
- 做什么：收到用户输入，生成task_id，将状态切换为PLANNING，调用TaskRouter。
- 转换条件：收到输入，生成id
- 下一状态：PLANNING

[状态: PLANNING]
- 做什么：收到TaskRouter返回的执行计划，将状态切换为ROUTING，取出第一个子任务的能力需求。
- 转换条件（成功/失败）：
    成功：收到子任务的capability，
    失败：TaskRouter返回错误（无法解析意图/无法生成执行计划）→ 进FAILED。
- 下一状态（成功/失败分别是什么）：ROUTING

[状态：ROUTING]
- 做什么：根据capability,寻找符合的大模型，将状态转为CALLING
- 转换条件（成功/失败）：
    成功：找到符合的大模型
    失败：如果Router返回NO_AVAILABLE_MODEL，直接进FAILED。
- 下一状态（成功/失败）：CALLING

[状态：CALLING]
- 做什么：调动大模型执行子任务，根据任务完成情况，将状态转为最终状态SUCCESS/PARTIAL_SUCCESS/FAILED
- 转换条件（成功/失败）：
    成功：  标记当前子任务完成 → 检查执行计划是否还有剩余子任务。
            有剩余 → 回到ROUTING（取下一个子任务）
            无剩余 → 进SUCCESS
    失败：（超时/401/503）→ 是否还有备选模型未尝试？
            有备选 → 回到ROUTING（重试当前子任务，但把失败模型名传给Router）
            无备选 → 检查是否有已成功的子任务。
            有 → 进PARTIAL_SUCCESS
            无 → 进FAILED
- 下一状态（成功/失败）：SUCCESS/PARTIAL_SUCCESS/FAILED

[状态：SUCCESS]
- 调用Builder拼装最终JSON，返回给前端。
- 转换条件（成功/失败）：
    成功：完成任务

[状态：PARTIAL_SUCCESS]
- 调用Builder拼装最终JSON，返回给前端。
- 转换条件（成功）：至少一个子任务成功 + 至少一个子任务彻底失败（所有备选耗尽）

[状态：FAILED]
- 调用Builder拼装最终JSON，返回给前端。
- 转换条件（失败）：所有子任务都彻底失败，无任何成功子任务


--------------------------------------------------------------
模块名：TaskRouter
职责：意图解析 + 任务拆解 + 生成执行计划
输入：user_input:string

输出：
    - 成功：
    {
        "intent": "multi_step_multimodal",
        "subtasks": [
            {"step": 1, "capability": "image-generation", "prompt": "未来城市的图片"},
            {"step": 2, "capability": "text-generation", "prompt": "描述这张图片"}
        ]
    }
    -失败：{
        "error": "INTENT_PARSE_FAILED"
    }
核心逻辑：
    1.解析用户输入的文本，识别意图类型（单步文本/单步多模态/多步多模态）   
    2.如果是多步任务，拆解为有序的子任务列表，每个子任务包含步骤序号、所需模型能力和生成该步骤的提示词
    3.如果是单步任务，返回只包含一个子任务的列表
    4.返回完整的执行计划（子任务列表）给Agent
    5.如果无法识别意图，返回INTENT_PARSE_FAILED



--------------------------------------------------------------------
模块名：llmClient
职责：封装HTTP调用，只负责请求/响应/超时

输入：
    {
        "model_name": "model-x",
        "endpoint": "http://...",
        "api_key": "...",
        "prompt": "生成一张未来城市的图片",
        "output_type": "image"
    }
输出：
    - 成功：
        {
            "status": "SUCCESS",
            "data": {
                "type": "image",
                "url": "/data/outputs/img_001.png"
            }
        }
    - 失败：
        {
            "status": "FAILED",
            "error_code": 503,
            "model_name": "model-x"
        }
核心逻辑：
    接收模型连接信息和提示词，构造HTTP请求
    发起调用，设置超时时间（默认30秒）
    收到响应后，解析返回内容并标准化为统一格式
    如果超时或返回错误状态码，返回失败状态和错误码
    不关心被调用的模型是首选还是备选，不关心重试逻辑,不负责调用链的汇总和拼接，每次调用独立返回结果。


--------------------------------------------------------------
模块名：Builder
职责：收集子任务结果，拼装统一JSON

输入：
    {
        "task_id": "uuid-xxxx",
        "final_status": "SUCCESS",
        "results": [
            {"type": "image", "url": "/data/outputs/img_001.png"},
            {"type": "text", "content": "这是一张未来城市的图片..."}
        ],
        "call_chain": [
            {"model_name": "model-x", "capability": "image-generation", "status": "SUCCESS", "attempted_at": "..."},
            {"model_name": "model-A", "capability": "text-generation", "status": "SUCCESS", "attempted_at": "..."}
        ]
    }
输出：
    - 成功（final_status为FALLBACK_SUCCESS）：
    {
        "frontend_response":{
            "task_id": "uuid-xxxx",
            "task_status": "SUCCESS",
            "system_status": "READY",
            "data": {
                "results": [
                    {"type": "image", "url": "/data/outputs/img_001.png"},
                    {"type": "text", "content": "这是一张未来城市的图片..."}
                ]
            }
        },
        "log_record":{
            "task_id":"uuid-xxxx",
            "timestamp":"(保存时间戳)",
            "task_status":"FALLBACK_SUCCESS",//若只有部分任务步骤返回成功则为PARTIAL_SUCCESS
            "system_status":"READY",
            "call_chain":[
                {
                    "model_name":"model-x",
                    "capability":"image-generation",
                    "status"::"FAILED",
                    "error_code":xxx,
                    "attempted_at":"(保存时间戳)"
                },//若第一个任务步骤的大模型成功则为SCUCCESS，且不走下一步调用链
                {
                    "model_name":"model-y",
                    "capability":"image-generation",
                    "status"::"SUCCESS",
                    "attempted_at":"(保存时间戳)"
                },
                {
                    "model_name":"model-A",
                    "capability":"text-generation",
                    "status"::"FAILED",
                    "error_code":xxx,
                    "attempted_at":"(保存时间戳)"
                },//若第二个任务步骤的大模型成功则为SCUCCESS，不走下一步调用链
                {
                    "model_name":"model-B",
                    "capability":"text-generation",
                    "status"::"SUCCESS",
                    "attempted_at":"(保存时间戳)"
                }
            ], //模型降级路线
            "error_summary": [
                {"step": 1, "type": "PRIMARY_MODEL_FAILED"},
                {"step": 2, "type": "PRIMARY_MODEL_FAILED"}
            ]
        }
    }

    - 成功（final_status为PARTIAL_SUCCESS）：
        {
            "frontend_response":{
                "task_id": "uuid-xxxx",
                "task_status": "PARTIAL_SUCCESS",
                "system_status": "READY",
                "data": {
                        "results": [
                            {"type": "image", "url": "/data/outputs/img_001.png"},
                            {"type": "text", "content": "这是一张未来城市的图片..."}
                        ]
                    }
            },
            "log_record":{
                    "task_id":"uuid-xxxx",
                    "timestamp":"(保存时间戳)"
                    "task_status":"PARTIAL_SUCCESS",
                    "system_status":"READY",
                    "call_chain":[
                      {
                          "model_name":"model-x",
                          "capability":"image-generation",
                          "status"::"FAILED",
                          "error_code":xxx,
                          "attempted_at":"(保存时间戳)"
                      },//若第一个任务步骤的大模型成功则为SCUCCESS，则不走下一步调用链
                      {
                          "model_name":"model-y",
                          "capability":"image-generation",
                          "status"::"SUCCESS",
                          "attempted_at":"(保存时间戳)"
                      },
                      {
                          "model_name":"model-A",
                          "capability":"text-generation",
                          "status"::"FAILED",
                          "error_code":xxx,
                          "attempted_at":"(保存时间戳)"
                      },
                      {
                          "model_name":"model-B",
                          "capability":"text-generation",
                          "status"::"FAILED",
                          "error_code":xxx
                          "attempted_at":"(保存时间戳)"
                      }
                  ], //模型降级路线
                  "error_summary": [
                      {"step": 1, "type": "PRIMARY_MODEL_FAILED"},
                      {"step": 2, "type": "ALL_MODEL_FAILED"}
                  ]
            }
        }


    - 失败（final_status为FAILED）：
        {
                "frontend_response":{
                "task_id": "uuid-xxxx",
                "task_status": "FAILED",
                "system_status": "READY",
                "data": {
                    "type": "text",
                    "message": "服务暂时不可用，请稍后重试"
                }
            },
            "log_record":{
                "task_id":"uuid-xxxx",
                "timestamp":"（保存时间戳）",
                "task_status":"FAILED",
                "system_status":"READY",
                "call_chain":[
                    {
                        "model_name":"model-x",
                        "capability":"image-generation",
                        "status":"FAILED",
                        "error_code":xxx,
                        "attempted_at":""
                    },
                    {
                        "model_name":"model-Y",
                        "capability":"image-generation",
                        "status":"FAILED",
                        "error_code":xxx,
                        "attempted_at":""
                    },
                    {
                        "model_name":"model-A",
                        "capability":"text-generation",
                        "status":"FAILED",
                        "error_code":xxx,
                        "attempted_at":""
                    },
                    {
                        "model_name":"model-B",
                        "capability":"text-generation",
                        "status":"FAILED",
                        "error_code":xxx,
                        "attempted_at":""
                    },
                ],
                "error_summary": [
                      {"step": 1, "type": "ALL_MODEL_FAILED"},
                      {"step": 2, "type": "ALL_MODEL_FAILED"}
                  ]
            }
        }
核心逻辑：
    接收Agent传入的task_id、最终状态、子任务结果列表和调用链
    根据final_status决定task_status的枚举值
    成功/部分成功时，将results数组放入data，部分成功时只放入成功的那部分结果
    失败时，生成用户可读的错误提示信息，不暴露内部错误码
    同时生成日志格式的JSON（包含call_chain和error_summary），返回前端JSON和日志JSON两份
    返回一个包含 frontend_response 和 log_record 的数据结构给Agent。

----------------------------------------------------------
模块名：Storage
职责：SQLite 持久化存储，统一管理对话历史与任务日志

数据表：
  - messages: 对话消息（session_id, role, content, created_at）
  - task_logs: 任务日志（task_id, session_id, task_status, frontend_json, log_json, created_at）

输入/输出：
  - save_message(session_id, role, content) → None
  - get_messages(session_id, limit) → List[dict]
  - save_task_log(task_id, session_id, frontend, log) → None

核心逻辑：
  1. 首次使用时自动建表
  2. 对话消息按 session_id 存储和查询
  3. 任务日志按 task_id 唯一存储，重复写入时覆盖
  4. 日志写入失败时静默处理，不影响主流程

----------------------------------------------------------
模块名：TaskManager
职责：定义任务存储的标准接口，抽象接口

抽象方法：
    - create_task(task_id: dict)->str
        """创建任务，生成唯一 task_id，初始化状态为 pending"""
        输入：{"user_input": "",
                "session_id": ""}
        输出：task_id: str

    - get_task(task_id: str)->dict
        """查询任务当前状态和结果"""
        输入：task_id
        输出：{
            "task_id": "", "status": "panding/processing/success/failed", "result":{}, "created_at": 
        }
    - update_task(task_id: str, status: str, result: dict = None) ->None
        """更新任务状态，status 为 pending / processing / success / failed"""
        输入：task_id, status, result

核心逻辑：
    1.定义三个抽象方法的签名，不提供具体实现    
    2.子类必须实现全部三个方法
    3.create_task 生成 task_id 并存储初始状态
    4.get_task 返回任务完整信息，若 task_id 不存在则返回 None
    5.update_task 更新状态和结果，写入后不可回滚
--------------------------------------------------------------------
模块名：RedisTaskManager (TaskManager)
职责：基于 Redis 实现任务队列和状态存储
输入输出：继承TaskManager
Redis 数据结构：
    agent:queue             List    任务队列，LPUSH 入队，BLPOP 出队
    agent:task: {task_id}   Hash    任务状态和结果，字段：status, result, created_at, user_input, session_id

核心逻辑：
    1.create_task：生成 UUID 作为 task_id，将任务数据序列化后 LPUSH 到 agent:queue，
        同时 HSET 初始化 agent:task:{task_id}，设置 EXPIRE 3600 秒
    2.get_task：HGETALL 获取 agent:task:{task_id} 所有字段，
        反序列化 result 字段（JSON），若 Key 不存在则返回 None
    3.update_task：HSET 更新 agent:task:{task_id} 的 status 和 result 字段
    4.所有 Redis 操作使用连接池管理连接，不每次创建新连接

----------------------------------------------------------------------
模块名：Worker
职责：独立进程，从 Redis 队列取任务，调用 Agent 执行，更新 Redis 状态和 SQLite 日志
输入：BLPOP，从Redis队列获取任务数据
输出：将结果写入Redis和SQLite
核心逻辑：

    启动时加载 Agent 和 Storage 实例（复用 load_agent() 和 Storage 初始化逻辑）

    进入无限循环：BLPOP agent:queue 阻塞等待任务，超时 5 秒后重试

    拿到任务后，调用 update_task(task_id, "processing") 更新状态

    调用 agent.run(user_input, session_id) 执行核心引擎

    执行完成后，调用 update_task(task_id, final_status, result) 写入 Redis

    同时调用 Storage.save_task_log(...) 和 Storage.save_message(...) 写入 SQLite

    若执行过程中 Worker 崩溃，Redis 中的任务状态停留在 processing，由超时机制兜底

------------------------------------------------------------------------
模块名：FastAPI 路由
职责：接收前端请求，提交任务队列，提供任务状态查询
端点：
    POST /api/task/submit   
        输入：{
                "user_input": "", 
                "session_id": ""
            }
        输出：{
            "task_id": "",
            "status": "panding"
        } + HTTP 202
    
    GET /api/task/{task_id}
        输入：task_id
        输出：{
            "task_id": "",
            "status": "",
            "result": ""
        } 无输出时返回404

核心逻辑：

    /api/task/submit：校验 user_input 非空，调用 TaskManager.create_task()，返回 202 和 task_id

    /api/task/{task_id}：调用 TaskManager.get_task()，存在则返回状态和结果，不存在则返回 404

    使用 FastAPI 框架，自动生成 OpenAPI 文档（/docs）

    不持有 Agent 实例——Agent 由 Worker 持有，FastAPI 只做任务提交和状态查询

==============================================================

====================================================
错误码枚举表：

错误码	                含义	            发生位置	         处理策略	        处理模块
503	                    服务不可用	    llmClient调用模型时	    切换备选模型	    Router
401	                    Key错误	        llmClient调用模型时	   切换备选模型     	Router
TIMEOUT	                调用超时	       llmClient	        切换备选模型        Router
INTENT_PARSE_FAILED	    意图识别失败	    TaskRouter	    返回失败,提示更换提示词  Agent
NO_AVAILABLE_MODEL	    无可用模型	        Router	          报错提示无可用模型    Agent
ALL_MODELS_EXHAUSTED	所有模型耗尽	 llmClient           任务失败，返回用户提示	 Agent

========================================================
配置错误的预防策略。:

措施一：环境隔离的配置文件
LLMClient 需要 endpoint 和 api_key，这些不能硬编码在代码里。
使用单独的配置文件，并按环境区分：config/dev.yaml、config/prod.yaml。
Agent 启动时根据环境变量 ENV 加载对应文件。这样测试环境的 Key 和地址永远不会污染生产，反过来也一样。

措施二：启动时的配置校验
Agent 启动后、进入 READY 状态之前，做一个 health_check 
步骤：遍历模型注册表中所有模型，用最轻量的请求（比如空查询）验证 endpoint 可达、api_key 有效。
校验失败的模型标记为 unavailable，记入日志但不阻断启动。
=

===================================================验证用例
模块名：Router
验证用例1（成功）：
    - 输入：capability_required="text-generation", failed_models=[]
    - 预期输出：返回已注册的text-generation模型中优先级最高的那个（model_name, endpoint, api_key）

验证用例2（失败）：
    - 输入：capability_required="text-generation", failed_models=["model-x", "model-y"]（假设只有x和y有该能力）
    - 预期输出：{"error": "NO_AVAILABLE_MODEL"}

-----------------------------------------------------------
模块名：Agent
验证用例 1（成功路径——单步任务直接成功）

    - 前置条件：系统处于 READY 状态，所有模块正常工作。
    - 输入：user_input = "单一任务"
    - 模拟内部过程：
        TaskRouter 返回一个单步子任务计划。
        Router 返回 model-x 的地址。
        LLMClient 返回成功
    - 预期输出：Agent 调用 Builder，Builder 返回统一成功 JSON。Agent 内部状态最终为 SUCCESS。

验证用例 2（降级路径——CALLING 失败但有备选模型）
    - 前置条件：系统注册了主模型 X 和备用模型 Y。本用例验证 Agent 能否正确触发 FALLBACK 并最终成功。
    - 输入：user_input = "单一任务"
    - 模拟内部过程：
        TaskRouter 返回单步子任务。
        第一次调 Router，Router 返回 X 的地址。
        第一次调 LLMClient，LLMClient 返回失败，错误码 503。
        Agent 判断有备选，将 X 加入失败列表，再次调用 Router。
        Router 返回 Y 的地址。
        第二次调 LLMClient，LLMClient 返回成功。
    - 预期输出：
        Agent 内部状态流转经过了 CALLING -> (失败) -> ROUTING -> CALLING -> SUCCESS。
        Agent 最终返回成功响应，用户无感知。

验证用例 3（失败路径——CALLING 失败且无备选模型）
    - 前置条件：系统只注册了主模型 X，无备用模型，或所有模型都已耗尽。
    - 输入：user_input = 单一任务
    - 模拟内部过程：
        TaskRouter 返回单步子任务。
        第一次调 Router，Router 返回 X 的地址。
        第一次调 LLMClient，LLMClient 返回失败，错误码 503。
        Agent 判断无备选模型（将 X 加入失败列表后再次调 Router，Router 返回 NO_AVAILABLE_MODEL）。
    - 预期输出：
        Agent 内部状态从 CALLING 直接转入 FAILED。
        Agent 最终返回失败响应，包含用户提示“服务暂时不可用”。

验证用例 4（PARTIAL_SUCCESS——多步任务第一步成功、第二步耗尽）
    - 前置条件：系统注册了生图模型（X/Y）和描述模型（A/B）。本用例验证多步任务的部分成功逻辑。
    - 输入：user_input = "生成未来城市图片并描述"
    - 模拟内部过程：
        TaskRouter 返回两步任务计划。
        执行第一步：Router 返回 X，LLMClient 成功返回图片。
        执行第二步：Router 返回 A，LLMClient 失败（503）。Agent 触发 FALLBACK，Router 返回 B，LLMClient 再次失败。
        Agent 判断第二步所有模型耗尽，且存在已成功的第一步。
    - 预期输出：
        Agent 内部状态进入 PARTIAL_SUCCESS。
        最终返回的部分成功响应里，data.results 只包含图片结果，不包含文本描述。

--------------------------------------------
模块名：TaskRouter
验证用例 1（成功）：
    - 输入：user_input:string
    - 预期输出：{
        "intent": "multi_step_multimodal",
        "subtasks": [
            {"step": 1, "capability": "image-generation", "prompt": "string"},
            {"step": 2, "capability": "text-generation", "prompt": "string"}
        ]
    }

验证用例2（失败）：
    - 输入：user_input：string
    - 预期输出：{"error": "INTENT_PARSE_FAILED"}

-----------------------------------------------------
模块名：llmClient
验证用例 1（成功）：
  - 输入：{
        "model_name": "model-X",
        "endpoint": "http://...",
        "api_key": "...",
        "prompt": "string",
        "output_type": "image"
    }
  - 预期输出：{
            "status": "SUCCESS",
            "data": {
                "type": "image",
                "url": "/data/outputs/img_001.png"
            }
        }

验证用例2（失败）：
    - 输入：{
        "model_name": "model-x",
        "endpoint": "http://...",
        "api_key": "...",
        "prompt": "string",
        "output_type": "image"
    }
    - 预期输出：{
            "status": "FAILED",
            "error_code": 503,
            "model_name": "model-x"
        }


------------------------------------------------------
模块名：Builder
验证用例 1（成功,final_status为SUCCESS）：：
  - 输入：{
        "task_id": "uuid-xxxx",
        "final_status": "SUCCESS",
        "results": [
            {"type": "image", "url": "/data/outputs/img_001.png"},
            {"type": "text", "content": "这是一张未来城市的图片..."}
        ],
        "call_chain": [
            {"model_name": "model-x", "capability": "image-generation", "status": "SUCCESS", "attempted_at": "..."},
            {"model_name": "model-A", "capability": "text-generation", "status": "SUCCESS", "attempted_at": "..."}
        ]
    }
  - 预期输出：{
        "frontend_response":{
            "task_id": "uuid-xxxx",
            "task_status": "FALLBACK_SUCCESS",
            "system_status": "READY",
            "data": {
                "results": [
                    {"type": "image", "url": "/data/outputs/img_001.png"},
                    {"type": "text", "content": "这是一张未来城市的图片..."}
                ]
            }
        },
        "log_record":{
            "task_id":"uuid-xxxx",
            "timestamp":"(保存时间戳)",
            "task_status":"FALLBACK_SUCCESS",//若只有部分任务步骤返回成功则为PARTIAL_SUCCESS
            "system_status":"READY",
            "call_chain":[
                {
                    "model_name":"model-x",
                    "capability":"image-generation",
                    "status"::"FAILED",
                    "error_code":xxx,
                    "attempted_at":"(保存时间戳)"
                },//若第一个任务步骤的大模型成功则为SCUCCESS，且不走下一步调用链
                {
                    "model_name":"model-y",
                    "capability":"image-generation",
                    "status"::"SUCCESS",
                    "attempted_at":"(保存时间戳)"
                },
                {
                    "model_name":"model-A",
                    "capability":"text-generation",
                    "status"::"FAILED",
                    "error_code":xxx,
                    "attempted_at":"(保存时间戳)"
                },//若第二个任务步骤的大模型成功则为SCUCCESS，不走下一步调用链
                {
                    "model_name":"model-B",
                    "capability":"text-generation",
                    "status"::"SUCCESS",
                    "attempted_at":"(保存时间戳)"
                }
            ], //模型降级路线
            "error_summary": [
                {"step": 1, "type": "PRIMARY_MODEL_FAILED"},
                {"step": 2, "type": "PRIMARY_MODEL_FAILED"}
            ]
        }
    }

验证用例 2（成功,final_status为PARTIAL_SUCCESS）：：
  - 输入：{
        "task_id": "uuid-xxxx",
        "final_status": "PARTIAL_SUCCESS",
        "results": [
            {"type": "image", "url": "/data/outputs/img_001.png"},
            {"type": "text", "content": "这是一张未来城市的图片..."}
        ],
        "call_chain": [
            {"model_name": "model-x", "capability": "image-generation", "status": "SUCCESS", "attempted_at": "..."},
            {"model_name": "model-A", "capability": "text-generation", "status": "SUCCESS", "attempted_at": "..."}
        ]
    }
    - 预期输出：{
            "frontend_response":{
                "task_id": "uuid-xxxx",
                "task_status": "PARTIAL_SUCCESS",
                "system_status": "READY",
                "data": {
                        "results": [
                            {"type": "image", "url": "/data/outputs/img_001.png"},
                            {"type": "text", "content": "这是一张未来城市的图片..."}
                        ]
                    }
            },
            "log_record":{
                    "task_id":"uuid-xxxx",
                    "timestamp":"(保存时间戳)"
                    "task_status":"PARTIAL_SUCCESS",
                    "system_status":"READY",
                    "call_chain":[
                      {
                          "model_name":"model-x",
                          "capability":"image-generation",
                          "status"::"FAILED",
                          "error_code":xxx,
                          "attempted_at":"(保存时间戳)"
                      },//若第一个任务步骤的大模型成功则为SCUCCESS，则不走下一步调用链
                      {
                          "model_name":"model-y",
                          "capability":"image-generation",
                          "status"::"SUCCESS",
                          "attempted_at":"(保存时间戳)"
                      },
                      {
                          "model_name":"model-A",
                          "capability":"text-generation",
                          "status"::"FAILED",
                          "error_code":xxx,
                          "attempted_at":"(保存时间戳)"
                      },
                      {
                          "model_name":"model-B",
                          "capability":"text-generation",
                          "status"::"FAILED",
                          "error_code":xxx
                          "attempted_at":"(保存时间戳)"
                      }
                  ], //模型降级路线
                  "error_summary": [
                      {"step": 1, "type": "PRIMARY_MODEL_FAILED"},
                      {"step": 2, "type": "ALL_MODEL_FAILED"}
                  ]
            }
        }

验证用例3（失败）：
    - 输入：
    {
        "task_id": "uuid-xxxx",
        "final_status": "FAILED",
        "results": [
            {"type": "image", "url": "/data/outputs/img_001.png"},
            {"type": "text", "content": "这是一张未来城市的图片..."}
        ],
        "call_chain": [
            {"model_name": "model-x", "capability": "image-generation", "status": "SUCCESS", "attempted_at": "..."},
            {"model_name": "model-A", "capability": "text-generation", "status": "SUCCESS", "attempted_at": "..."}
        ]
    }
    - 预期输出：{
                "frontend_response":{
                "task_id": "uuid-xxxx",
                "task_status": "FAILED",
                "system_status": "READY",
                "data": {
                    "type": "text",
                    "message": "服务暂时不可用，请稍后重试"
                }
            },
            "log_record":{
                "task_id":"uuid-xxxx",
                "timestamp":"（保存时间戳）",
                "task_status":"FAILED",
                "system_status":"READY",
                "call_chain":[
                    {
                        "model_name":"model-X",
                        "capability":"image-generation",
                        "status":"FAILED",
                        "error_code":xxx,
                        "attempted_at":""
                    },
                    {
                        "model_name":"model-Y",
                        "capability":"image-generation",
                        "status":"FAILED",
                        "error_code":xxx,
                        "attempted_at":""
                    },
                    {
                        "model_name":"model-A",
                        "capability":"text-generation",
                        "status":"FAILED",
                        "error_code":xxx,
                        "attempted_at":""
                    },
                    {
                        "model_name":"model-B",
                        "capability":"text-generation",
                        "status":"FAILED",
                        "error_code":xxx,
                        "attempted_at":""
                    },
                ],
                "error_summary": [
                      {"step": 1, "type": "ALL_MODEL_FAILED"},
                      {"step": 2, "type": "ALL_MODEL_FAILED"}
                  ]
            }
        }
---------------------------------------
模块名：Storage
职责：SQLite 持久化存储，统一管理对话历史与任务日志

验证用例 1（成功——保存并读取对话消息）

    输入：

    调用 save_message(session_id="test-001", role="user", content="Hello")

    调用 save_message(session_id="test-001", role="assistant", content="你好")

    调用 get_messages(session_id="test-001", limit=10)

    预期输出：返回包含两条消息的列表，第一条 role 为 "user"、content 为 "Hello"，第二条 role 为 "assistant"、content 为 "你好"

验证用例 2（成功——保存任务日志）

    输入：
    
    调用 save_task_log(task_id="task-001", session_id="test-001", frontend={...}, log={...})
    
    用 SQLite 客户端直接查询 SELECT * FROM task_logs WHERE task_id='task-001'
    
    预期输出：查询到一条记录，task_id 为 "task-001"，task_status 与传入的 log 中的状态一致，frontend_json 和 log_json 为合法的 JSON 字符串

验证用例 3（成功——数据库文件自动创建）

    前置条件：删除已有的 data/agent.db 文件

    输入：初始化 Storage(db_path="data/agent.db")

    预期输出：data/agent.db 文件被自动创建，且包含 messages 和 task_logs 两张表

验证用例 4（失败——日志写入失败不影响主流程）

    前置条件：使用一个不可写的路径初始化 Storage（如 /root/readonly/agent.db，或无权限的目录）

    输入：调用 save_task_log(...)

    预期输出：方法正常返回，不抛出异常。get_messages() 和 save_message() 同理，各自静默处理错误

----------------------------------------------------------
Worker 模块：

验证用例1（成功）：启动 Worker，向 agent:queue 手动 LPUSH 一个简单文本任务，验证 Worker 能正确消费并更新 Redis 状态为 success

验证用例2（失败）：传入一个会导致 Agent 失败的输入（如无效模型），验证 Worker 更新 Redis 状态为 failed，并且 SQLite 日志中记录了错误

---------------------------------------------------------------
RedisTaskManager 模块：

验证用例1（成功）：调用 create_task → 验证 Redis 中存在对应 Key，且状态为 pending

验证用例2（成功）：调用 update_task → 再调用 get_task → 验证状态和结果一致

验证用例3（失败）：调用 get_task 传入不存在的 task_id → 返回 None

-----------------------------------------------------------------
FastAPI 路由层：

验证用例1（成功）：POST /api/task/submit → 返回 202 和 task_id

验证用例2（成功）：GET /api/task/{task_id} → 返回当前状态

验证用例3（失败）：GET /api/task/nonexistent → 返回 404